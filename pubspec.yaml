name: mio_ani
description: "MioAni cross-platform anime discovery and library application."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  flutter_web_plugins:
    sdk: flutter
  go_router: ^17.3.0

dev_dependencies:
  build_runner: 2.15.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  go_router_builder: ^4.4.0

flutter:
  uses-material-design: true
